<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.theantiquersroom.myapp.mapper.ProductMapper">

    <insert id="inserProduct"
            parameterType="com.theantiquersroom.myapp.domain.ProductFormDTO">
        <selectKey keyProperty="productId" resultType="int" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    <![CDATA[
            insert into product(name, content, category_id, user_id)
            VALUES (#{name}, #{content}, #{categoryId}, #{userId})
        ]]>
    </insert>
    <insert id="inserAuction"
            parameterType="com.theantiquersroom.myapp.domain.ProductFormDTO">
    <![CDATA[
            insert into auction(auction_id, started_at, ended_at, started_price, bid_increment, status)
            VALUES (#{productId} ,#{startedAt}, #{endedAt}, #{startedPrice}, #{bidIncrement}, "REQUESTED")
        ]]>
    </insert>

</mapper>